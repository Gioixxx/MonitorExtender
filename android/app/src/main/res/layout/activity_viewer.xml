<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- La view si misura con le proporzioni del frame, quindi si centra invece di riempire. -->
    <com.monitorextender.viewer.MjpegSurfaceView
        android:id="@+id/video"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center" />

    <!-- Misure sopra il video: stesso monospazio e stessa ambra della schermata di scelta. -->
    <TextView
        android:id="@+id/stats"
        style="@style/Readout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="20dp"
        android:background="@drawable/overlay_panel"
        android:paddingStart="14dp"
        android:paddingTop="10dp"
        android:paddingEnd="14dp"
        android:paddingBottom="10dp"
        android:textColor="@color/signal"
        android:textSize="12sp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/status"
        style="@style/Readout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/overlay_panel"
        android:paddingStart="24dp"
        android:paddingTop="18dp"
        android:paddingEnd="24dp"
        android:paddingBottom="18dp"
        android:textColor="@color/ink"
        android:textSize="14sp"
        android:visibility="gone" />

</FrameLayout>
